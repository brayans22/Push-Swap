#include "push_swap.h"

int main(int argc, char **argv)
{
    if (argc == 0)
        return 0;
    //t_stack *stack_a;
    //t_stack *stack_b;

    //stack_a = NULL;
    //stack_b = NULL;
    if (!is_valid_input(argc, argv))
        return (ERROR);
    /* ADD ELEMENTS TO THE STACK A */

    return 0;
}